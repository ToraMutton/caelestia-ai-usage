#!/usr/bin/env python3
"""Launcher: works from any cwd and through a symlink (e.g. ~/.local/bin/ai-usage)."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.realpath(__file__))))

from ai_usage.cli import main  # noqa: E402

sys.exit(main())
